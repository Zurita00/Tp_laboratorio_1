
#include <stdio.h>

/** \brief Funcion creada para sumar dos valores flotantes.
 *
 * \param Valor flotante
 * \param  "
 * \return Devuelve la suma de ambos.
 *
 */
float sumar (  float, float);

/** \brief Funcion creada para restar dos valores flotantes.
 *
 * \param Valor flotante
 * \param "
 * \return Devuelve la resta de ambos.
 *
 */
float restar ( float, float);

/** \brief Funcion creada para multiplicar.
 *
 * \param Valor float.
 * \param  "
 * \return Devuelve la multiplicacion de estos valores
 */
float multiplicar ( float, float);

/** \brief Funcion creada para dividir dos valores flotantes.
 *
 * \param Valor flotante
 * \param  "
 * \return Devuelve la division de ambos.
 *
 */
float dividir ( float, float);


/** \brief Funcion para realizar el factorial de un flotante
 *
 * \param Valor flotante
 * \return Retorna un valor flotante
 */
float factorial ( float);
